import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

const friendsStore = new Vuex.Store({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  },
});

export default friendsStore;
